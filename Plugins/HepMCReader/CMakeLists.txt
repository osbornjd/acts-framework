if(USE_HEPMC)
  file(GLOB src_files "src/HepMCReader.cpp" "include/ACTFW/Plugins/HepMCReader/HepMCReader.hpp")
  add_library(ACTFWPluginHepMCReader SHARED ${src_files})
  target_include_directories(ACTFWPluginHepMCReader PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include/> $<INSTALL_INTERFACE:include>)
  target_link_libraries(ACTFWPluginHepMCReader PUBLIC ACTSCore ACTFramework)
  target_link_libraries(ACTFWPluginHepMCReader PRIVATE HepMC) 
  target_link_libraries(ACTFWPluginHepMCReader PRIVATE HepPID)

  install(TARGETS ACTFWPluginHepMCReader LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR})
  install(DIRECTORY include/ACTFW DESTINATION ${CMAKE_INSTALL_INCLUDEDIR})
endif()
if(USE_HEPMC3)
  file(GLOB src_files "src/HepMC3*.cpp" "include/ACTFW/Plugins/HepMCReader/HepMC3*.hpp")
  add_library(ACTFWPluginHepMC3Reader SHARED ${src_files})
  target_include_directories(ACTFWPluginHepMC3Reader PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include/> $<INSTALL_INTERFACE:include>)
  target_link_libraries(ACTFWPluginHepMC3Reader PUBLIC ACTSCore ACTFramework)
  target_link_libraries(ACTFWPluginHepMC3Reader PRIVATE HepMC3) 
  target_link_libraries(ACTFWPluginHepMC3Reader PRIVATE HepPID)

  install(TARGETS ACTFWPluginHepMC3Reader LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR})
  install(DIRECTORY include/ACTFW DESTINATION ${CMAKE_INSTALL_INCLUDEDIR})
endif()
